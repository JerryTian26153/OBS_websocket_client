// 此為回到最上層按鈕的JS
{
    edit_page = document.getElementById("chat_manager")
    document.getElementsByClassName("edit_button").onclick = function(e) {
        var positionX = e.clientX + 'px';
        var positionY = e.clientX + 'px';
        el.style.top = position;
    };
}